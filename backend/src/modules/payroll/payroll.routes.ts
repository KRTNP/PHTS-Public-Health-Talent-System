/**
 * Compatibility shim.
 *
 * Canonical route module lives at `api/payroll.route.ts`.
 * Keep this file until import migration is complete.
 */

export { default } from "@/modules/payroll/api/payroll.route.js";
